/**
 * Placeholder module for ATL-007 (toolchain bootstrap).
 * The real React app shell (src/app/ per docs/FOLDER_STRUCTURE.md) lands in M1.
 */
export function workspaceName(): string {
  return "@atlas/web";
}
